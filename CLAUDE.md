# wisaw プロジェクト概要

結婚式への招待状を AWS を用いたWebサービスとして提供するためのプロジェクト。  
ほぼ使い切り、かつ小規模、かつ利用者も利用期間も限定的なサービスである。  
このため、サーバーレスな実行環境やシンプルな設計(言語は Typescript のみ、モノレポ、など)を採用する。

## 実現したいこと

- ユーザーにいくつかの設問に回答してもらう
- 回答を DB に保存する
- ユーザーにメールを送信する
- 写真集的なものやミニゲームなどのその他のコンテンツをついでに提供する
- 以上をWebサービスとして提供する

## 使用ツール
- `pnpm -r build`: プロジェクトのトップディレクトリから配下のサブプロジェクトすべてに対して再帰的にビルドを実行する
- `pnpm dev:web`: ローカルホストにWebサービスを展開する
- `./aws_setup.sh`: バックエンドとして使用する AWS の DynamoDB などをローカルで立ち上げるための準備を行う

## 全体構成

このプロジェクトはモノレポである。つまり、 pnpm workspace によって apps/* と packages/* を束ねてフロントエンド、バックエンドともに単一のリポジトリでシステム全てを実現する。  

- フロントエンド
    - AWS CloudFront & S3 で `apps/web` 配下のプロジェクトからなるWebページをホスティングする
    - `apps/web` は React 19 + Vite + Tailwind CSS v4 な Typescript で実装する
    - UI コンポーネントには MynaUI (shadcn) を用いる
- バックエンド
    - AWS Lambda(SAM) で DynamoDB を使った `apps/api` 配下のプロジェクトで駆動する
    - フロントエンドからは HTTP API でデータを受取る。データは Zod スキーマでバリデーションを取る
- 共有モジュール
    - フロントエンドとバックエンドでのデータ連携に使う Zod スキーマとバリデーション関数などを `packages/shared` で実装する
- ローカル環境
    - AWS そのものを利用せずにローカルだけで可能な限りの開発を行うための環境を用意する
    - フロントエンドは `localhost:5173` に Vite dev server を展開し、 SAM API は Docker ネットワークで DynamoDB Local に接続する
    - バックエンドは DynamoDB Local (Docker) + SAM CLI で構成する

## 設計原則

- 開発言語
    - フロントエンド/バックエンドともに Typescript を採用する
        - フロントエンドは Typescript 以外ありえない
        - バックエンドにもフロントエンドと同じ Typescript を採用することでデータ連携の摩擦を低減する
            - 小規模なプログラムのため、無理のない範疇で実現可能と判断した
- フレームワーク
    - フロントエンドは React を用いることで相互作用を実装する
        - 最も普及しているライブラリと認識しているため
    - フロントエンドの CSS フレームワークには Tailwind CSS を採用する
        - CSS そのものを書くのは嫌なため
        - 十分な機能性を完備し、かつネットに情報量が多いため
    - UI コンポーネントには MynaUI (shadcn) を用いる
        - UI デザインの一貫性を保つために、これ以外のコンポーネントは原則使わない。自作も極力避ける。
- API
    - Write Only とする(Post/submit only)
    - 冪乗キーによって二重送信を防止する
- インフラ
    - AWS のみを使用する
        - 開発者の経験として AWS を使いたかったから。技術的な理由というより、学習という側面が強い
